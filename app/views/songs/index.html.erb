<h1>These are all the Songs</h1>
<%= button_to "Add New Song", new_song_path, method: "get" %>
<ul>
    <% @songs.each do |song| %>
    <li>
        <%= link_to song.name, song %>
    </li>
    <% end %>
</ul>

